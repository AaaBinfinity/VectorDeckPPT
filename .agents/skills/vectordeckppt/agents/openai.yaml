interface:
  display_name: "VectorDeckPPT"
  short_description: "Create editable vector-first PowerPoint presentations"
  default_prompt: "Use $vectordeckppt to first confirm a complete request contract and a user-chosen, uploaded, or explicitly delegated visual source, then create an information-rich, art-directed editable deck, approve the text and an up-to-three-slide sample, and validate SVG, typography, previews, and PPTX."
